const SERVO_MOTORS = [
  {
    pin: 18,
    minPulse: 501,
    maxPulse: 2200,
    minDegrees: 0,
    maxDegrees: 180,
    speed: 0.1,
  },
  {
    pin: 19,
    minPulse: 501,
    maxPulse: 2200,
    minDegrees: 0,
    maxDegrees: 180,
    speed: 0.1,
  },
];

module.exports = SERVO_MOTORS;
